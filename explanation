Got it ✅ — below are two separate metadata extracts for your 2 SQL codes, in the exact same format and rules you have been using.
(Only persistent Snowflake tables, only columns actually referenced)

⸻

✅ Metadata Extraction — SQL Code 1

Database	Schema	Base Table Name	Attributes Needed
CORE_FSSC	CURATED_ORDERS	ORDER_DELIVERY	ORDER_ID, STORE_NBR, TRACKING_NBR, CREATE_USER_NM, DELIVERY_SLA_CD, REC_EFF_TS
CORE_FSSC	CURATED_ORDERS	ORDER_DELIVERY_ITEM	ORDER_ID, STORE_NBR, RX_NBR, FILL_NBR
CORE_FSSC	CURATED_ORDERS	ORDER_DELIVERY_HISTORY	ORDER_ID, STORE_NBR, ORDER_STATUS_CD, REC_EFF_TS, TRACKING_NBR, CREATE_USER_NM, DELIVERY_SLA_CD
CORE_RX	CURATED_SENSITIVE	EMPLOYEE	EMPLOYEE_ID, FIRST_NM, LAST_NM, JOB_TITLE_NM

✅ Valid — No temp CTEs included
✅ No computed columns included
✅ Only columns used in SELECT / GROUP BY / WHERE / LISTAGG / CASE

⸻

✅ Metadata Extraction — SQL Code 2

Database	Schema	Base Table	Attributes Needed
CORE_FSSC	CURATED_ORDERS	ORDER_DELIVERY	ORDER_ID, STORE_NBR, TRACKING_NBR, CREATE_USER_NM, DELIVERY_SLA_CD, REC_EFF_TS
CORE_FSSC	CURATED_ORDERS	ORDER_DELIVERY_HISTORY	ORDER_ID, STORE_NBR, ORDER_STATUS_CD, REC_EFF_TS, TRACKING_NBR, CREATE_USER_NM, DELIVERY_SLA_CD
CORE_FSSC	CURATED_ORDERS	ORDER_DELIVERY_ITEM	ORDER_ID, STORE_NBR, RX_NBR, FILL_NBR
CORE_RX	CURATED_SENSITIVE	EMPLOYEE	EMPLOYEE_ID, FIRST_NM, LAST_NM, JOB_TITLE_NM
CORE_FSSC	CURATED_CALENDAR	DAY	DATE_DT, FISCAL_WEEK_NBR


⸻

✅ Notes on Differences

Area	SQL Code 1	SQL Code 2
Calendar Table	❌ Not used	✅ Used
Time Buckets	Label and shipping TS	Quarter + Hour labels
Aggregation Level	Script delivery details	Colleague productivity rollups


⸻

📌 Status of Metadata Captured So Far

You have now provided 8 Snowflake SQL processes ✅
All metadata successfully extracted in identical format.

⸻

✅ Next Step (Pick One)

A — Merge all Snowflake metadata into one consolidated dictionary
B — Keep each script separately documented (as we are doing now)
C — Export a CSV file for Snowflake metadata (and later IDW + RXCOE_PMI)

Reply with A, B, or C ✅

⸻

Would you also like me to create:

📌 A Script → Data Source Lineage Map (visual flow diagram)?
📌 Field usage frequency report (top used columns across all CF analytics)?

Just say:

Yes, include lineage diagram ✅
