
Perfect — extracted and formatted exactly like all previous metadata ✅

(Only real Snowflake base tables, no CTEs)

⸻

✅ Extracted Metadata for This CF Activity / Productivity Query

Database	Schema	Base Table Name	Attributes Needed
CORE_RX	CURATED_SCRIPT	PRESCRIPTION_FILL_ACTIVITY	RXC_PRESCRIPTION_FILL_ID, ACTIVITY_CD, EFFECTIVE_TS, REC_EFF_TS, CREATE_USER, FIRST_NM, LAST_NM, USER_CREDENTIALS, EMPLOYEE_ID
CORE_RX	CURATED_SCRIPT	CENTRAL_FILL_RX_ASSIGNMENT	PRESCRIPTION_FILL_ID, CENTRAL_FILL_FACILITY_ID, REC_EFF_TS
CORE_RX	CURATED_SCRIPT	PRESCRIPTION_FILL_XREF	RXC_PRESCRIPTION_FILL_ID, STORE_NBR, RX_NBR, FILL_NBR
CORE_RX	CURATED_SENSITIVE	EMPLOYEE	EMPLOYEE_ID, JOB_TITLE_NM
CORE_FSSC	CURATED_CALENDAR	DAY	DATE_DT, FISCAL_WEEK_NBR


⸻

✅ Notes

✔ Fields included only if used in SELECT, JOIN, WHERE, GROUP BY, HAVING
✔ Derived values (hour_label, quarter label, SUM, ROUND…) are excluded as expected
✔ Temp CTEs CF_DATA and TEMP ignored (correct behavior)

⸻

We now have six Snowflake metadata extractions completed ✅

Before continuing:

Would you like your Snowflake metadata consolidated into a single master sheet?

Reply with an option:

A — Merge into one consolidated Snowflake metadata catalog
B — Keep script-by-script metadata segments separate
C — Export as downloadable CSV file(s)
  • Snowflake_metadata.csv
  • IDW_metadata.csv
  • RXCOE_PMI_metadata.csv

Just reply with A, B, or C ✅
